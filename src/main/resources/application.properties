spring.application.name=sdoktorom-BE

# PostgreSQL (odkomentuj iba ak pou?íva? PostgreSQL)
# spring.datasource.url=jdbc:postgresql://localhost:5432/sdoktorom
# spring.datasource.username=postgres
# spring.datasource.password=heslo
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# OpenAI nastavenia (k?ú? NEPATRI do gitu, len do lokálneho/dev/prod servera)
OPENAI_API_KEY=sk-xxxx...
openai.api.url=https://api.openai.com/v1/chat/completions

# H2 databáza (pou?ívaj pri developmente, hlavne ak testuje? ?asto)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

